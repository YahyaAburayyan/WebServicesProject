"""
Staff module router stub.

All staff, survey, assignment, and analytics endpoints live in
app/modules/analytics/router.py (Student 3's combined router).
This file is intentionally left as an empty router to avoid duplicate
route registration.
"""

from fastapi import APIRouter

router = APIRouter(tags=["Staff (stub — see analytics router)"])
